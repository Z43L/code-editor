// Archivo de prueba para comandos de IA
function calculateSum(a, b) {
    return a + b;
}

function calculateProduct(x, y) {
    return x * y;
}

// Función con un error intencional
function divideNumbers(num1, num2) {
    return num1 / num2; // No maneja división por cero
}

console.log("Test file created");